<UserControl x:Class="SportFactory.MembersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Add/Edit Member Form -->
        <StackPanel Grid.Row="0" Margin="10">
            <xctk:WatermarkTextBox x:Name="FirstNameTextBox" Watermark="Prenom" Margin="0,0,0,5"/>
            <xctk:WatermarkTextBox x:Name="LastNameTextBox" Watermark="Last Name" Margin="0,0,0,5"/>
            <DatePicker x:Name="StartDatePicker" Margin="0,0,0,5"/>
           
        </StackPanel>

        <!-- Member List View -->
        <ListView Grid.Row="1" x:Name="MembersListView" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Prenom" DisplayMemberBinding="{Binding FirstName}" Width="120"/>
                    <GridViewColumn Header="Nom" DisplayMemberBinding="{Binding LastName}" Width="120"/>
                    <GridViewColumn Header="Start Date" DisplayMemberBinding="{Binding StartDate}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
